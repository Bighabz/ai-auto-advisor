---
phase: 03-multi-platform-gateway
plan: "03"
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - skills/shared/conversation.js
autonomous: true
requirements:
  - PLAT-02
  - DLVR-03

must_haves:
  truths:
    - "SAM's system prompt instructs Claude to ask 'Delete just the estimate, or also the customer record?' before calling cleanup_estimate"
    - "buildSystemPrompt() output contains text about customer record deletion choice"
    - "test-conversation.js DLVR-03 cleanup test turns GREEN after this plan"
    - "Existing conversation.js tests remain GREEN — no regression"
  artifacts:
    - path: "skills/shared/conversation.js"
      provides: "System prompt with cleanup customer-ask instruction"
      contains: "customer record"
  key_links:
    - from: "skills/shared/conversation.js buildSystemPrompt()"
      to: "cleanup_estimate tool delete_customer_vehicle parameter"
      via: "system prompt instruction before tool call"
      pattern: "customer record"
---

<objective>
Add one system prompt instruction to conversation.js so Claude asks the tech whether to delete just the estimate or also the customer record before calling cleanup_estimate. This satisfies the locked user decision from CONTEXT.md.

Purpose: DLVR-03 (cleanup with confirmation) and PLAT-02 (same cleanup behavior on both platforms — driven by conversation.js, not gateway-specific).
Output: A small targeted addition to buildSystemPrompt() in conversation.js. No other changes.
</objective>

<execution_context>
@C:/Users/habib/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/habib/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-multi-platform-gateway/03-CONTEXT.md
@.planning/phases/03-multi-platform-gateway/03-RESEARCH.md
@.planning/phases/03-multi-platform-gateway/03-01-SUMMARY.md

<interfaces>
<!-- Key code location the executor needs. -->

From skills/shared/conversation.js — buildSystemPrompt() (lines ~123-170):
The function builds a long string. The current system prompt ends with:
```
FORMATTING:
- *bold* for key numbers and headers (Telegram/WhatsApp Markdown)
- Keep chat responses SHORT
- Line breaks for readability`
```

Then optionally appends lastEstimate context if provided.

The cleanup_estimate tool (already built in conversation.js) has a parameter:
```javascript
delete_customer_vehicle: {
  type: "boolean",
  description: "Also delete the customer and vehicle records (not just the estimate RO)",
}
```

From RESEARCH.md open question #2:
> Recommendation: Add one instruction to system prompt: "Before calling cleanup_estimate, ask the user: 'Delete just the estimate, or also the customer record?' Then use their answer to set delete_customer_vehicle."

From CONTEXT.md locked decision:
> When deleting, ask each time: "Delete just the estimate, or also the customer record?"
> Same flow on both Telegram and WhatsApp — no platform differences in cleanup behavior.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add cleanup customer-ask instruction to buildSystemPrompt()</name>
  <files>skills/shared/conversation.js</files>
  <behavior>
    - buildSystemPrompt(null) result includes the phrase "customer record" (or "delete_customer_vehicle")
    - buildSystemPrompt(null) result includes instruction to ask before calling cleanup_estimate
    - The instruction is in a CLEANUP COMMANDS section (or appended to INTENT ROUTING section)
    - Existing system prompt content is unchanged — new section appended only
    - test-conversation.js DLVR-03 test passes after this change
  </behavior>
  <action>
    Open skills/shared/conversation.js and locate the buildSystemPrompt() function.

    Find the end of the main prompt string (just before the closing backtick of the template literal, around the FORMATTING section). Insert a new section before the closing backtick:

    ```
CLEANUP COMMANDS:
- When a user says "delete that", "delete the estimate", "remove that test", or similar cleanup language, call the cleanup_estimate tool.
- Before calling cleanup_estimate, ALWAYS ask: "Delete just the estimate, or also the customer record?" Wait for the user's answer before calling the tool.
- Use their answer to set delete_customer_vehicle: true (if they say "customer too", "both", "everything") or false (if they say "just the estimate", "estimate only", "just that").
- The tool will show a confirmation with full details (RO#, customer name, vehicle, total) before deleting. This confirmation happens inside the tool — do not add a separate preview.
    ```

    This instruction:
    - Is per the locked user decision: "Delete just the estimate, or also the customer record?"
    - Makes the ask happen before the tool call (per RESEARCH.md recommendation)
    - Works identically on Telegram and WhatsApp since it lives in the shared engine
    - Does NOT change the tool's two-step preview/confirm behavior — that is unchanged

    Do NOT modify any other part of conversation.js. This is a surgical addition only.
  </action>
  <verify>
    <automated>node tests/unit/test-conversation.js 2>&1 | tail -15 && node tests/unit/run.js 2>&1 | tail -5</automated>
  </verify>
  <done>test-conversation.js DLVR-03 test line shows PASS. All existing test-conversation.js tests still pass. node tests/unit/run.js full suite GREEN. buildSystemPrompt() output includes "customer record".</done>
</task>

</tasks>

<verification>
- node -e "const c = require('./skills/shared/conversation'); console.log(c.buildSystemPrompt(null).includes('customer record'))" → true
- node tests/unit/test-conversation.js — DLVR-03 test PASS, all others still PASS
- node tests/unit/run.js — full suite GREEN (all plans complete at this point)
</verification>

<success_criteria>
Plan 03 complete when:
- [ ] buildSystemPrompt() includes instruction to ask about customer record deletion
- [ ] test-conversation.js DLVR-03 cleanup test GREEN
- [ ] All existing tests still pass — no regression
- [ ] node tests/unit/run.js full suite GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/03-multi-platform-gateway/03-03-SUMMARY.md`
</output>
