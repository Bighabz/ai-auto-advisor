---
phase: 01-queue-and-session-foundation
plan: 04
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - skills/telegram-gateway/scripts/server.js
  - skills/ai-diagnostics/scripts/diagnose.js
  - skills/autoleap-browser/scripts/helpers/motor-nav.js
autonomous: true
requirements: [MODEL-01, MODEL-02, MODEL-03]

must_haves:
  truths:
    - "No file in the codebase contains the string 'claude-3-haiku-20240307' (retired April 19, 2026)"
    - "No file contains a hardcoded 'claude-sonnet-4-5-20250929' string (replaced with env var)"
    - "server.js uses process.env.CLAUDE_SONNET_MODEL with 'claude-sonnet-4-6' default"
    - "diagnose.js CLAUDE_MODEL constant uses process.env.CLAUDE_SONNET_MODEL with 'claude-sonnet-4-6' default"
    - "motor-nav.js model field uses process.env.CLAUDE_HAIKU_MODEL with 'claude-haiku-4-5-20251001' default"
  artifacts:
    - path: "skills/telegram-gateway/scripts/server.js"
      provides: "Gateway Claude calls with env var model selection"
      contains: "process.env.CLAUDE_SONNET_MODEL"
    - path: "skills/ai-diagnostics/scripts/diagnose.js"
      provides: "Diagnostic synthesis with env var model selection"
      contains: "process.env.CLAUDE_SONNET_MODEL"
    - path: "skills/autoleap-browser/scripts/helpers/motor-nav.js"
      provides: "MOTOR category selection with env var model selection"
      contains: "process.env.CLAUDE_HAIKU_MODEL"
  key_links:
    - from: "server.js model field"
      to: "process.env.CLAUDE_SONNET_MODEL"
      via: "|| fallback to 'claude-sonnet-4-6'"
      pattern: "CLAUDE_SONNET_MODEL.*claude-sonnet-4-6"
    - from: "diagnose.js CLAUDE_MODEL const"
      to: "process.env.CLAUDE_SONNET_MODEL"
      via: "|| fallback to 'claude-sonnet-4-6'"
      pattern: "CLAUDE_SONNET_MODEL.*claude-sonnet-4-6"
    - from: "motor-nav.js model field"
      to: "process.env.CLAUDE_HAIKU_MODEL"
      via: "|| fallback to 'claude-haiku-4-5-20251001'"
      pattern: "CLAUDE_HAIKU_MODEL.*claude-haiku-4-5-20251001"
---

<objective>
Replace all hardcoded Claude model ID strings in three source files with environment variable lookups. This is a purely mechanical one-to-two-line change per file, critical before the April 19, 2026 `claude-3-haiku-20240307` retirement deadline.

Purpose: Decouple model version from code. After this plan, operators can upgrade the Claude model by setting an env var without a code deploy. The deprecated `claude-haiku` model (April 19 retirement) is no longer hardcoded anywhere.
Output: Three modified source files. test-model-ids.js turns green.
</objective>

<execution_context>
@C:/Users/habib/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/habib/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/01-queue-and-session-foundation/01-RESEARCH.md
@tests/unit/test-model-ids.js
</context>

<interfaces>
<!-- Exact line locations from codebase inspection (RESEARCH.md Sources section): -->
<!-- server.js line 219:     model: "claude-sonnet-4-5-20250929", -->
<!-- diagnose.js line 34:    const CLAUDE_MODEL = "claude-sonnet-4-5-20250929"; -->
<!-- motor-nav.js line 1288: model: "claude-haiku-4-5-20251001", -->

<!-- Required replacements: -->
```javascript
// server.js line 219 — BEFORE:
model: "claude-sonnet-4-5-20250929",

// server.js line 219 — AFTER:
model: process.env.CLAUDE_SONNET_MODEL || "claude-sonnet-4-6",

// diagnose.js line 34 — BEFORE:
const CLAUDE_MODEL = "claude-sonnet-4-5-20250929";

// diagnose.js line 34 — AFTER:
const CLAUDE_MODEL = process.env.CLAUDE_SONNET_MODEL || "claude-sonnet-4-6";

// motor-nav.js line 1288 — BEFORE:
model: "claude-haiku-4-5-20251001",

// motor-nav.js line 1288 — AFTER:
model: process.env.CLAUDE_HAIKU_MODEL || "claude-haiku-4-5-20251001",
```
<!-- Note: motor-nav.js already uses the CORRECT non-deprecated model. -->
<!-- The change adds env var flexibility but does NOT change the default behavior. -->
<!-- Note from research pitfall 6: motor-nav.js builds a raw JSON body (not the SDK). -->
<!-- The model field is inside a JSON object literal, same syntax as the other files. -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Apply model ID env var substitution to all three files</name>
  <files>skills/telegram-gateway/scripts/server.js, skills/ai-diagnostics/scripts/diagnose.js, skills/autoleap-browser/scripts/helpers/motor-nav.js</files>
  <action>
    Make exactly three targeted substitutions — one per file. Read each file first to confirm the line before editing.

    **server.js:**
    Find the line containing `model: "claude-sonnet-4-5-20250929"` (near line 219, inside the Anthropic API call in `processMessage`). Replace the string literal with `process.env.CLAUDE_SONNET_MODEL || "claude-sonnet-4-6"`.

    **diagnose.js:**
    Find the line `const CLAUDE_MODEL = "claude-sonnet-4-5-20250929";` (near line 34, module-level constant). Replace the string literal with `process.env.CLAUDE_SONNET_MODEL || "claude-sonnet-4-6"`.

    **motor-nav.js:**
    Find the line `model: "claude-haiku-4-5-20251001",` (near line 1288, inside the raw HTTPS request body object). Replace with `model: process.env.CLAUDE_HAIKU_MODEL || "claude-haiku-4-5-20251001",`.

    Do NOT make any other changes to these files. Do NOT reformat, reorganize, or touch any other lines.

    After all three edits, verify no deprecated model string remains:
    `grep -r "claude-3-haiku-20240307\|claude-sonnet-4-5-20250929" skills/ --include="*.js"`
    Expected: no output (zero matches).
  </action>
  <verify>
    <automated>node tests/unit/test-model-ids.js</automated>
  </verify>
  <done>test-model-ids.js exits 0. All 7 model ID tests pass: three "no deprecated string" checks and three "env var present" checks. Zero matches for grep of deprecated model IDs.</done>
</task>

</tasks>

<verification>
```bash
node tests/unit/test-model-ids.js
node tests/unit/run.js
```

Expected: test-model-ids.js turns GREEN (was failing in Plan 01-01). Full suite: all suites pass (test-model-ids now green, all others were already green).
</verification>

<success_criteria>
- `grep -r "claude-3-haiku-20240307" skills/` returns zero results
- `grep -r '"claude-sonnet-4-5-20250929"' skills/` returns zero results
- All three files contain their respective `process.env.CLAUDE_*_MODEL || "..."` pattern
- `node tests/unit/test-model-ids.js` exits 0
- `node tests/unit/run.js` exits 0 (full suite green)
</success_criteria>

<output>
After completion, create `.planning/phases/01-queue-and-session-foundation/01-04-SUMMARY.md`
</output>
