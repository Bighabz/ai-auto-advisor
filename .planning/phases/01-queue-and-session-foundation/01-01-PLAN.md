---
phase: 01-queue-and-session-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/test-job-queue.js
  - tests/unit/test-session-store.js
  - tests/unit/test-model-ids.js
autonomous: true
requirements: [QUEUE-01, QUEUE-02, QUEUE-03, QUEUE-04, SESS-01, SESS-02, SESS-03, MODEL-01, MODEL-02, MODEL-03]

must_haves:
  truths:
    - "Running `node tests/unit/run.js` exits 0 with all three new test files reporting PASS"
    - "Each test file covers its requirement group and fails loudly on wrong behavior"
    - "test-model-ids.js greps source files for hardcoded deprecated model IDs and reports them"
  artifacts:
    - path: "tests/unit/test-job-queue.js"
      provides: "QUEUE-01 through QUEUE-04 test coverage"
    - path: "tests/unit/test-session-store.js"
      provides: "SESS-01 through SESS-03 test coverage"
    - path: "tests/unit/test-model-ids.js"
      provides: "MODEL-01 through MODEL-03 test coverage"
  key_links:
    - from: "tests/unit/run.js"
      to: "tests/unit/test-*.js"
      via: "auto-discovery (files starting with test-)"
      pattern: "f.startsWith\\(\"test-\"\\)"
---

<objective>
Create the three test scaffold files required before any implementation can be verified. These are the Wave 0 Nyquist files — every subsequent plan's `<automated>` verify command points to one of these files.

Purpose: Establish the verification contract for all Phase 1 requirements before writing a line of implementation. Tests are written against the expected interface so implementation is guided by the test.
Output: Three test files, all passing (initial stubs), auto-discovered by `tests/unit/run.js`.
</objective>

<execution_context>
@C:/Users/habib/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/habib/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@tests/unit/run.js
@tests/unit/test-session-manager.js
@.planning/phases/01-queue-and-session-foundation/01-VALIDATION.md
</context>

<interfaces>
<!-- Existing test runner pattern — from tests/unit/run.js -->
<!-- Auto-discovers test-*.js files in tests/unit/, runs each via execSync, exit 1 on any failure -->
<!-- Each test file must be a standalone Node.js script (no framework) -->
<!-- Pattern from test-session-manager.js: -->
```javascript
function assert(cond, msg) { if (!cond) throw new Error("FAIL: " + msg); }
async function runTests() {
  const tests = [];
  let pass = 0, fail = 0;
  function test(name, fn) { tests.push({ name, fn }); }
  // ... test() calls ...
  for (const { name, fn } of tests) {
    try { await fn(); pass++; console.log(`  PASS: ${name}`); }
    catch (e) { fail++; console.error(`  FAIL: ${name} — ${e.message}`); }
  }
  console.log(`\nResults: ${pass} passed, ${fail} failed`);
  if (fail > 0) process.exit(1);
}
runTests().catch(e => { console.error(e); process.exit(1); });
```
<!-- The modules being tested do NOT exist yet — use stubs/mocks where needed -->
<!-- test-model-ids.js should NOT require non-existent modules; grep file contents directly -->
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write test-job-queue.js scaffold (QUEUE-01 through QUEUE-04)</name>
  <files>tests/unit/test-job-queue.js</files>
  <behavior>
    - Test: single enqueue runs immediately (queue.pending === 1 during run)
    - Test: second enqueue while first is running returns { alreadyQueued: false } and job resolves after first
    - Test: after first job completes, second job auto-starts (QUEUE-03)
    - Test: getStatus() returns { status: "running" } for active job, null for unknown user
    - Test: double-enqueue same userId is idempotent (returns { alreadyQueued: true, position: N })
    - Test: notifyPosition callback is called BEFORE queue.add when queue.size > 0
    - Initial scaffold: since job-queue.js does not exist yet, stub the module path — these tests will fail (MISSING module) until Plan 01-03 creates the module. Write tests to PASS once the module is correctly implemented. Use a try/catch guard at the top: if require fails, skip all tests with a WARNING message and exit 0 (so the suite does not break Wave 1).
  </behavior>
  <action>
    Create `tests/unit/test-job-queue.js`. Structure:

    1. At the top, try to require `../../skills/shared/job-queue`. If it throws (module not yet created), print "WARNING: job-queue.js not yet created — skipping" and process.exit(0). This allows Wave 1 plans to run without a broken test suite.

    2. Write tests against this interface (to be implemented in Plan 01-03):
       - `enqueueEstimate(userId, jobFn, { notifyPosition })` → returns Promise resolving to jobFn's return value
       - `getStatus(userId)` → returns `{ status: "queued"|"running", position, queuedAt }` or null
       - `queue` (the PQueue instance) is exported for direct inspection in tests

    3. Test cases (all using in-process async with fake jobFns — no real Chrome):
       - "single job runs immediately": enqueue one job, confirm it resolves with the correct return value
       - "second job queues and runs after first": start a slow job (returns after 50ms), immediately enqueue second — verify second gets `alreadyQueued: false`, both resolve in sequence
       - "getStatus returns running during execution": check `getStatus(userId).status === "running"` inside the first job's async body
       - "idempotent re-enqueue": call enqueueEstimate for same userId twice while first is running — second call returns `{ alreadyQueued: true }`
       - "notifyPosition called when queue has waiters": spy on notifyPosition; start first job, enqueue second — verify spy was called with (position, waitMinutes)
       - "getStatus returns null after completion": after job resolves, `getStatus(userId)` returns null

    Log prefix: `[test-job-queue]`
  </action>
  <verify>
    <automated>node tests/unit/test-job-queue.js</automated>
  </verify>
  <done>Script exits 0. If job-queue.js is absent, exits 0 with WARNING. Once job-queue.js exists (Plan 01-03), all 6 tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write test-session-store.js and test-model-ids.js scaffolds</name>
  <files>tests/unit/test-session-store.js, tests/unit/test-model-ids.js</files>
  <behavior>
    test-session-store.js:
    - Test: getSession returns null for unknown key
    - Test: setSession then getSession (memory path) returns the same object
    - Test: setSession writes platform, chat_id fields correctly
    - Test: cleanupExpiredSessions deletes rows older than 24h (use Supabase mock or skip if no credentials)
    - Test: in-memory fallback: when Supabase env vars absent, getSession/setSession work via memCache only
    - Test: makeKey("telegram", "123") === "telegram:123"

    test-model-ids.js:
    - Test: server.js does NOT contain "claude-3-haiku-20240307" (deprecated model string)
    - Test: server.js does NOT contain hardcoded "claude-sonnet-4-5-20250929" (use env var instead)
    - Test: diagnose.js does NOT contain hardcoded "claude-sonnet-4-5-20250929"
    - Test: motor-nav.js does NOT contain hardcoded "claude-3-haiku-20240307"
    - Test: server.js contains "process.env.CLAUDE_SONNET_MODEL" string
    - Test: diagnose.js contains "process.env.CLAUDE_SONNET_MODEL" string
    - Test: motor-nav.js contains "process.env.CLAUDE_HAIKU_MODEL" string
  </behavior>
  <action>
    **test-session-store.js:**

    Same try/require guard as Task 1 — if `../../skills/shared/session-store` does not exist, print WARNING and exit 0.

    For Supabase integration tests: check if `process.env.SUPABASE_URL` is set. If not, skip Supabase round-trip tests with "SKIP (no SUPABASE_URL)" log and still pass.

    For the mock path (no env vars), test that:
    - `makeKey("telegram", "1385723011")` returns `"telegram:1385723011"`
    - `setSession("telegram", "test-123", { lastEstimate: { ro: "16389" }, history: [], stage: "done", collectedData: {} })` resolves without error
    - `getSession("telegram", "test-123")` returns `{ lastEstimate: { ro: "16389" }, history: [], stage: "done", collectedData: {} }` from memory cache (no Supabase needed)
    - `deleteSession("telegram", "test-123")` resolves, then `getSession` returns null
    - `cleanupExpiredSessions()` returns `{ deleted: 0 }` when Supabase is null (graceful)

    **test-model-ids.js:**

    This test does NOT require any modules — it reads source files directly using `fs.readFileSync`. No try/require guard needed.

    File paths (relative to `cwd` which run.js sets to project root `tests/unit/../..`):
    - `skills/telegram-gateway/scripts/server.js`
    - `skills/ai-diagnostics/scripts/diagnose.js`
    - `skills/autoleap-browser/scripts/helpers/motor-nav.js`

    For each source file, use `fs.readFileSync(path, "utf8")` and check string presence/absence with `.includes()`.

    These tests will FAIL immediately (before Plan 01-04) because the deprecated/hardcoded strings still exist. That is intentional — they turn green after Plan 01-04 runs.

    Write tests so they report clearly which file still has a problem:
    - `assert(!server.includes("claude-3-haiku-20240307"), "server.js must not contain deprecated claude-3-haiku")`
    - `assert(!server.includes('"claude-sonnet-4-5-20250929"'), "server.js model must be env var, not hardcoded")`
    - etc.

    Log prefix: `[test-session-store]` / `[test-model-ids]`
  </action>
  <verify>
    <automated>node tests/unit/test-session-store.js && node tests/unit/test-model-ids.js</automated>
  </verify>
  <done>test-session-store.js exits 0 (session-store.js absent → WARNING+skip, or passes all in-memory tests). test-model-ids.js exits 1 with clear messages about which hardcoded strings still exist — this is expected RED state before Plan 01-04.</done>
</task>

</tasks>

<verification>
Run the full suite to confirm new test files are discovered and no existing tests are broken:
`node tests/unit/run.js`

Expected: All existing test suites still pass. New suites exit 0 (test-job-queue and test-session-store skip gracefully; test-model-ids fails — intentional RED before Plan 01-04).
</verification>

<success_criteria>
- `tests/unit/test-job-queue.js` exists, exits 0 (module-absent guard in place)
- `tests/unit/test-session-store.js` exists, exits 0 (module-absent guard in place)
- `tests/unit/test-model-ids.js` exists, fails with clear messages about hardcoded model strings (expected RED)
- `node tests/unit/run.js` reports at most 1 failed suite (test-model-ids) — all others pass
- No existing test files broken
</success_criteria>

<output>
After completion, create `.planning/phases/01-queue-and-session-foundation/01-01-SUMMARY.md`
</output>
