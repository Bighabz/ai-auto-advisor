---
phase: 01-queue-and-session-foundation
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - skills/shared/job-queue.js
autonomous: true
requirements: [QUEUE-01, QUEUE-02, QUEUE-03, QUEUE-04]

must_haves:
  truths:
    - "Only one buildEstimate() can run at a time — second enqueue waits in queue"
    - "User gets their queue position BEFORE queue.add() is called (message arrives immediately)"
    - "After the running job completes, the next queued job starts automatically"
    - "getStatus() returns running/queued state; returns null after job completes"
    - "Double-enqueue for same userId returns { alreadyQueued: true } without adding a second job"
  artifacts:
    - path: "skills/shared/job-queue.js"
      provides: "Singleton serial job queue with EventEmitter"
      exports: [enqueueEstimate, getStatus, emitter, queue]
  key_links:
    - from: "skills/shared/job-queue.js"
      to: "PQueue instance (concurrency: 1)"
      via: "require('@esm2cjs/p-queue') with Node 22 require(esm) fallback"
      pattern: "new PQueue\\(\\{ concurrency: 1 \\}\\)"
    - from: "enqueueEstimate"
      to: "activeJobs Map"
      via: "synchronous set BEFORE queue.add() to prevent double-enqueue race"
      pattern: "activeJobs.set.*synchronously"
---

<objective>
Build `skills/shared/job-queue.js` — a singleton in-memory serial job queue using `p-queue` with concurrency: 1. Both gateways import this one module so they share a single queue instance within the gateway process.

Purpose: Prevent concurrent `buildEstimate()` calls from corrupting the shared Chrome browser session on the Pi. When a second request arrives while one pipeline is running, the user is told their position and the job queues automatically.
Output: `job-queue.js` module passing all QUEUE-01–QUEUE-04 tests.
</objective>

<execution_context>
@C:/Users/habib/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/habib/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/01-queue-and-session-foundation/01-RESEARCH.md
@tests/unit/test-job-queue.js
@skills/shared/health.js
</context>

<interfaces>
<!-- p-queue install: npm install @esm2cjs/p-queue (on Pi: run in /home/sam/ai-auto-advisor) -->
<!-- p-queue v9 size vs pending semantics (critical — from research pitfall 1): -->
<!--   queue.size   = jobs WAITING (not running) -->
<!--   queue.pending = jobs currently RUNNING (max 1 with concurrency:1) -->
<!--   User's position = queue.size + 1, captured BEFORE queue.add() -->
<!--   Wait estimate = queue.size × 15 minutes (each job ~15 min) -->

<!-- Node.js 22 require(esm) is available — fallback pattern: -->
```javascript
let PQueue;
try {
  PQueue = require("@esm2cjs/p-queue").default;
} catch {
  const mod = require("p-queue");
  PQueue = mod.default || mod;
}
```

<!-- EventEmitter events emitted by the queue: -->
<!--   job:start   { userId } -->
<!--   job:complete { userId, result } -->
<!--   job:error   { userId, error } -->

<!-- activeJobs Map MUST be set synchronously before queue.add() to prevent double-enqueue race -->
<!-- (Node.js is single-threaded — synchronous set between event loop ticks is safe) -->
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement skills/shared/job-queue.js</name>
  <files>skills/shared/job-queue.js</files>
  <behavior>
    - enqueueEstimate("user1", slowFn, {}) resolves to slowFn's return value
    - getStatus("user1") returns { status: "running" } while job is active
    - getStatus("user1") returns null after job completes
    - Two jobs for same userId: second enqueue returns { alreadyQueued: true, position: 1 }
    - Two jobs for different userIds: both execute sequentially (concurrency: 1)
    - notifyPosition(pos, waitMin) is called with correct values when queue has waiters
    - emitter emits "job:complete" with { userId, result } on success
    - emitter emits "job:error" with { userId, error } when jobFn throws (error propagates to caller)
  </behavior>
  <action>
    Create `skills/shared/job-queue.js` following RESEARCH.md Pattern 1 exactly.

    Implementation steps:

    1. **p-queue import with fallback** (from interfaces block above).

    2. **Module-level singletons:**
       ```javascript
       const queue = new PQueue({ concurrency: 1 });
       const emitter = new EventEmitter();
       const activeJobs = new Map(); // userId -> { status, position, queuedAt, startedAt }
       ```

    3. **enqueueEstimate(userId, jobFn, { notifyPosition } = {}):**
       - Check `activeJobs.has(userId)`. If true → return `{ alreadyQueued: true, position: activeJobs.get(userId).position }` immediately. Do NOT add to queue.
       - Capture `const position = queue.size + 1` and `const waitMinutes = queue.size * 15` BEFORE modifying anything.
       - Set `activeJobs.set(userId, { status: "queued", position, queuedAt: Date.now() })` synchronously (prevents double-enqueue race).
       - If `queue.size > 0` (there are waiters ahead) AND `notifyPosition` is provided: call `await notifyPosition(position, waitMinutes)`. Note: when the second job arrives, `queue.size` is 0 BUT `queue.pending` is 1. Position is still 2 (the second person in line). Fix: position should be `queue.size + queue.pending + 1` to account for the running job. Use `queue.size + queue.pending + 1` for position; use `(queue.size + queue.pending) * 15` for wait minutes. Call `notifyPosition` whenever `queue.pending > 0 || queue.size > 0`.
       - Call `queue.add(async () => { ... })` and return its Promise.
       - Inside the queue callback: update `activeJobs.set(userId, { status: "running", startedAt: Date.now() })`, emit `"job:start"`, try jobFn(), emit `"job:complete"` on success, emit `"job:error"` on failure, and in `finally`: `activeJobs.delete(userId)`.

    4. **getStatus(userId):** Returns `activeJobs.get(userId) || null`.

    5. **Exports:** `module.exports = { enqueueEstimate, getStatus, emitter, queue };`

    After implementing, run the tests. If `@esm2cjs/p-queue` is not installed, the test will fail — install it first: `npm install @esm2cjs/p-queue` (run from project root `/home/sam/ai-auto-advisor` on Pi). On the dev machine, run from `C:\Users\habib\Downloads\sam\ai-auto-advisor`.
  </action>
  <verify>
    <automated>node tests/unit/test-job-queue.js</automated>
  </verify>
  <done>test-job-queue.js exits 0. All 6 tests pass: single run, sequential two-job, getStatus running, idempotent re-enqueue, notifyPosition callback, null after completion.</done>
</task>

</tasks>

<verification>
```bash
node tests/unit/test-job-queue.js
node tests/unit/run.js
```

Expected: test-job-queue.js 6 tests PASS. Full suite: test-model-ids still red (expected until Plan 01-04), all others green.
</verification>

<success_criteria>
- `skills/shared/job-queue.js` exists and exports enqueueEstimate, getStatus, emitter, queue
- Concurrency is exactly 1 (single Chrome pipeline at a time)
- `node tests/unit/test-job-queue.js` exits 0, 6 tests pass
- activeJobs cleared after each job (no memory leak)
</success_criteria>

<output>
After completion, create `.planning/phases/01-queue-and-session-foundation/01-03-SUMMARY.md`
</output>
